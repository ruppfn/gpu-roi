{
  "name": "gpu-roi",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "gpu-roi-front": "file:./src/main/js",
    "react-scripts": "5.0.0"
  },
  "scripts": {
    "build": "cd ./src/main/js && npm run build"
  }
}
