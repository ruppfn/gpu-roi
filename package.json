{
  "name": "gpu-roi",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.26.0",
    "cheerio": "^1.0.0-rc.10"
  },
  "devDependencies": {
    "aws-sdk": "^2.1078.0"
  },
  "scripts": {
    "prebuild": "cd ./src/main/js && npm install",
    "build": "cd ./src/main/js && npm run build && npm run add-to-java"
  }
}
